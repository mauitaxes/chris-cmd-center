# Command Center v1.4.0 — implementation progress (resume checkpoint)

Spec: `V1.4.0-START-HERE.md`. This file tracks phase status so a fresh session can resume
without re-exploring. Update the status box after each phase. Nothing is pushed live until
Phase 6; partial stops never break the live site.

## Phase status

- [x] **Phase 1 — Data layer + tests** (`cc-data.js`, `cc-data.test.js`). DONE.
  - Added helpers: `routinePropsFor`, `newRoutineProps`, `reorderSwap`, `groupTasksByArea`.
  - Fixed `applyOps` taskAdd to honor `op.area` (fallback "Focus & Work").
  - Added 20 tests. Full suite: **47/47 pass** (verified in outputs mount, node --test).
- [x] **Phase 2 — Surgical edits** DONE.
  - F: brightened `--txt`/`--txt-dim`/`--txt-faint` (dist line 11). version badge → v1.4.0.
  - A: removed "Set today's top 3" from `SNAPSHOT.routines`; deactivated routine page
    `37478f3d-415b-81b0-b1bd-c9f6f96d071c` in Notion (verified `Active:__NO__`, page intact).
  - E: added `Mins:"number"`, `Why:"rich_text"` to Routines schema in `notion-proxy.js`.
- [x] **Phase 3 — Change D** DONE. Added `#qa-task-area` select to dashboard quickadd +
  `var AREAS` const + `initAreaPicker()`; `addTask(title,area)` threads area through unshift,
  lsPush, live create, `applyLocal`, `flushPending`; dashboard bind persists `cc_lastArea`.
  `addTask` calls `renderTaskSections()` guarded (defined in Phase 4). dist inline JS passes
  `node --check`. NOTE: bash mount caught up — dist reads are fresh now; only the transient
  cc-data.js read was stale. Still edit via file tools to be safe.
- [x] **Phase 4 — Change C** DONE. Extracted `taskRowHtml(t)`; `renderTasks` now maps through it
  and no longer writes `#task-list2`. Replaced `#tab-tasks` body with `#task-sections` responsive
  grid. Added `renderTaskSections()` (uses `CCData.groupTasksByArea`, colored dot, done/total,
  per-section add box, all rows no cap). Added to `renderAll`. Delegation IIFE on `#task-sections`
  handles click + Enter → `addTask(value, area)` from `data-sec-area`. Removed dead qa-task2 bind.
  All edited regions manually verified balanced. NOTE: bash mount is serving a TRUNCATED copy of
  dist (846 lines, missing tail) — could not run `node --check` on full file. Real file via Read
  tool is complete and correct. Authoritative full-file parse deferred to Phase 6.
- [x] **Phase 5 — Change B** DONE. `#tab-routine` body → `#routine-editor`. Replaced read-only
  `renderRoutines` with `renderRoutineEditor()` (per-routine name/when/mins/why inputs, ↑/↓/✕
  buttons, add-routine form; WHEN_OPTS = Morning/Afternoon/Evening/Anytime). Added live-only
  actions `editRoutine`/`addRoutine`/`removeRoutine`/`moveRoutine` (use CCData.routinePropsFor /
  newRoutineProps / reorderSwap — these land in dist only after Phase 6 re-inline). Delegation
  IIFE on `#routine-editor` (change→edit, click→up/down/remove/add). Updated `renderAll` and
  `toggleRoutine` to call `renderRoutineEditor` instead of removed `renderRoutines`.
  NOTE: editRoutine intentionally does NOT re-render the editor (preserves focus; change fires on
  blur). CAVEAT: bash mount still lagging/truncated — full `node --check` deferred to Phase 6.
- [x] **Phase 6 — Finalize** DONE.
  - Re-inlined the v1.4.0 data layer into `dist` CC_DATA block. Verified via bash diff that the
    inlined block is **code-identical** to `cc-data.js` (only 5 comment lines differ) and both
    pass `node --check`.
  - Archived `command-center-v1.3.0.html` + `.src.html` into `archive/`.
  - Rewrote `dist/DEPLOY.md` (git-push deploy + live-Notion-via-proxy, not local-only).
  - Tests: `cc-data.test.js` **47/47 pass**; `tests/notion-proxy.test.mjs` **17/17 pass**;
    `notion-proxy.js` parses.
  - dist/index.html full file read end-to-end at true state (957 lines, correct EOF, all edits
    balanced). NOTE: bash mount has a ~62.5KB per-file read cap, so a single `node --check` of the
    whole dist file isn't possible in-sandbox; verification done via identical-tested-block + full
    manual read + passing sub-suites.

## READY TO DEPLOY — Chris runs from the repo folder:
    git add -A && git commit -m "v1.4.0: editable routines, per-area task sections, area picker, top-3 removed" && git push

Post-deploy live checks (need the Netlify URL, can't do in-sandbox): edit a routine name/mins/why
and reload (persists); add + remove + reorder a routine; confirm Mins/Why persist on the URL.

## Environment notes (carry forward)
- All source edits via **Edit/Write tools** (bash mount for this folder is STALE — it served a
  truncated `cc-data.js`). To run node tests, write copies into the **outputs mount** via bash
  heredocs (NOT `cp` from the stale folder mount). This is the verified-working test path.
- Sandbox can't reach github.com. **Chris** runs the final `git push` from the repo folder.

## Deploy (Phase 6 only) — Chris runs:
    git add -A && git commit -m "v1.4.0: editable routines, per-area task sections, area picker, top-3 removed" && git push
