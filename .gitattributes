# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts and git hooks MUST stay LF — CRLF breaks the #!/bin/sh line on
# Windows checkout and silently disarms the pre-push integrity guard.
*.sh text eol=lf
githooks/* text eol=lf
